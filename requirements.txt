Flask==2.3.3
Flask-SQLAlchemy==3.0.5
Flask-Migrate==4.0.5
Flask-JWT-Extended==4.5.2
Flask-CORS==4.0.0
python-dotenv==1.0.0
marshmallow==3.20.1
# 2.9.7 es de 2023 y NO publica ruedas para Python 3.12: pip se pone a
# compilar desde el código fuente y pide pg_config, que no está si no tienes
# PostgreSQL instalado en el sistema. 2.9.9 sí las trae, y con el suelo de
# macOS más bajo (10.9 en Intel, 11 en Apple Silicon), así que le sirve a
# cualquier máquina del equipo. La 2.9.10 existe pero exige macOS 14 en arm64.
psycopg2-binary==2.9.9
# Columnas Geography y funciones PostGIS (ST_DWithin, ST_AsGeoJSON) desde
# SQLAlchemy. Necesaria para Place.geom y LakeGeometry del sprint 1.3.
GeoAlchemy2==0.20.0
requests==2.31.0
# stripe 5.16.0 no existe en PyPI: `pip install -r requirements.txt` fallaba
# en limpio. Todavía no se importa en ningún sitio (app/payments/providers.py
# es la costura y está sin implementar), así que se fija la estable actual.
stripe==15.6.1

# Generación y verificación del contrato OpenAPI (spec.py y tests/test_contract.py).
# No las usa la app en runtime: solo la suite y el generador del spec.
apispec==6.10.0
PyYAML==6.0.2
jsonschema==4.23.0
openapi-spec-validator==0.7.1
